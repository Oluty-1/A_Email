---
- name: Mail via Ansible
  hosts: myserver
  become: yes
  gather_facts: yes
  
  tasks:
    - name: Install Apache
      apt:
        name: apache2
        state: present

    - name: Start Apache service
      service:
        name: apache2
        state: started
        enabled: yes

    - name: Sending an e-mail using Gmail SMTP servers
      mail:
        host: smtp.mailtrap.io
        port: 587
        username: a62936fb5ec3ff
        password: e6a01f8611bf26
        to: tayoefun@gmail.com
        subject: Ansible Task Completion Notification
        body: Your .
      delegate_to: localhost